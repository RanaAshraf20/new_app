import 'package:flutter/material.dart';

class AddNewContact extends StatelessWidget {
  const AddNewContact({super.key});
  static String id = 'AddNewContact';
  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
